// Returns error if no session file
// Loads session file
// Fetches mock HTML from URL
// Returns Error if the mock URL in the fethecd HTML is different than the one in the session file
// Examines status
// Pipes the status to the Error
